[project]
name = "eth-da-collector"
version = "0.1.0"
description = "Ethereum DA liveness collector"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
